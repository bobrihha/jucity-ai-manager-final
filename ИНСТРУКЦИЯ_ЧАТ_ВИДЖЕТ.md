# 📋 Инструкция для программиста: Установка чат-виджета на сайт nn.jucity.ru

## Обзор

Чат-виджет "Джуси" — интерактивный помощник в правом нижнем углу сайта. Позволяет задавать вопросы о парке, ценах и организации праздников.

---

## ⚠️ ПРЕДВАРИТЕЛЬНЫЙ ШАГ: Настройка домена

**Прежде чем устанавливать виджет на сайт, необходимо создать поддомен для API чата!**

### Что нужно сделать:

1. **Создать DNS-запись** (A-запись) для поддомена:
   - **Поддомен**: `chat.nn.jucity.ru` (или любое другое имя, например `bot.nn.jucity.ru`)
   - **IP-адрес**: `95.81.99.32`

2. **Сообщить нам выбранный поддомен**, чтобы мы:
   - Настроили SSL-сертификат (HTTPS)
   - Обновили конфигурацию nginx

3. **После настройки SSL** мы сообщим итоговый URL, например:
   - `https://chat.nn.jucity.ru/chat`

> ⚠️ **Важно**: Без HTTPS виджет не будет работать на сайте с HTTPS, так как браузеры блокируют "mixed content" (HTTP-запросы с HTTPS-страниц).

---

## Шаг 1: Вставить CSS стили в `<head>`

Добавьте стили перед закрывающим тегом `</head>` на **всех страницах сайта**:

```css
/* JUNGLE CITY CHAT WIDGET */
#jc-chat-button {
    position: fixed; bottom: 20px; right: 20px;
    width: 60px; height: 60px; border-radius: 50%;
    background: linear-gradient(135deg, #4CAF50 0%, #2E7D32 100%);
    border: none; cursor: pointer;
    box-shadow: 0 4px 15px rgba(76, 175, 80, 0.4);
    z-index: 9999; display: flex; align-items: center; justify-content: center;
    transition: transform 0.3s, box-shadow 0.3s;
}
#jc-chat-button:hover { transform: scale(1.1); box-shadow: 0 6px 20px rgba(76, 175, 80, 0.5); }
#jc-chat-button svg { width: 28px; height: 28px; fill: white; }
#jc-chat-badge {
    position: absolute; top: -5px; right: -5px;
    width: 20px; height: 20px; background: #FF5722;
    border-radius: 50%; display: none; align-items: center;
    justify-content: center; font-size: 12px; color: white; font-weight: bold;
}
#jc-chat-window {
    position: fixed; bottom: 90px; right: 20px;
    width: 380px; height: 550px; background: #fff;
    border-radius: 16px; box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    z-index: 9998; display: none; flex-direction: column;
    overflow: hidden; animation: jcSlideUp 0.3s ease-out;
}
@keyframes jcSlideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
#jc-chat-header {
    background: linear-gradient(135deg, #4CAF50 0%, #2E7D32 100%);
    color: white; padding: 16px 20px; display: flex; align-items: center; gap: 12px;
}
#jc-chat-avatar { width: 45px; height: 45px; border-radius: 50%; background: white; display: flex; align-items: center; justify-content: center; font-size: 24px; }
#jc-chat-info h3 { font-size: 16px; font-weight: 600; margin: 0 0 2px 0; }
#jc-chat-info p { font-size: 12px; opacity: 0.9; margin: 0; }
#jc-chat-close { margin-left: auto; background: none; border: none; color: white; font-size: 24px; cursor: pointer; opacity: 0.8; }
#jc-chat-close:hover { opacity: 1; }
#jc-chat-messages { flex: 1; overflow-y: auto; padding: 20px; background: #f5f7fa; display: flex; flex-direction: column; gap: 12px; }
.jc-message { max-width: 85%; padding: 12px 16px; border-radius: 16px; font-size: 14px; line-height: 1.5; animation: jcFadeIn 0.3s ease; }
@keyframes jcFadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
.jc-message.bot { background: white; color: #333; border-bottom-left-radius: 4px; align-self: flex-start; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
.jc-message.user { background: linear-gradient(135deg, #4CAF50 0%, #2E7D32 100%); color: white; border-bottom-right-radius: 4px; align-self: flex-end; }
.jc-message.typing { background: white; padding: 16px; }
.jc-typing-dots { display: flex; gap: 4px; }
.jc-typing-dots span { width: 8px; height: 8px; background: #4CAF50; border-radius: 50%; animation: jcTyping 1.4s infinite both; }
.jc-typing-dots span:nth-child(2) { animation-delay: 0.2s; }
.jc-typing-dots span:nth-child(3) { animation-delay: 0.4s; }
@keyframes jcTyping { 0%, 100% { opacity: 0.3; transform: scale(0.8); } 50% { opacity: 1; transform: scale(1); } }
.jc-quick-replies { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 10px; }
.jc-quick-reply { background: #e8f5e9; color: #2E7D32; border: 1px solid #4CAF50; padding: 8px 16px; border-radius: 20px; font-size: 13px; cursor: pointer; }
.jc-quick-reply:hover { background: #4CAF50; color: white; }
#jc-chat-input-area { padding: 16px; background: white; border-top: 1px solid #eee; display: flex; gap: 10px; }
#jc-chat-input { flex: 1; padding: 12px 16px; border: 1px solid #ddd; border-radius: 24px; font-size: 14px; outline: none; }
#jc-chat-input:focus { border-color: #4CAF50; }
#jc-chat-send { width: 44px; height: 44px; border-radius: 50%; background: linear-gradient(135deg, #4CAF50 0%, #2E7D32 100%); border: none; cursor: pointer; display: flex; align-items: center; justify-content: center; }
#jc-chat-send svg { width: 20px; height: 20px; fill: white; }
#jc-chat-send:disabled { opacity: 0.5; cursor: not-allowed; }
@media (max-width: 420px) { #jc-chat-window { width: 100%; height: 100%; bottom: 0; right: 0; border-radius: 0; } }
```

---

## Шаг 2: Вставить HTML разметку перед `</body>`

```html
<!-- JUNGLE CITY CHAT WIDGET -->
<button id="jc-chat-button" onclick="JCChat.toggle()">
    <svg viewBox="0 0 24 24"><path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H6l-2 2V4h16v12z"/></svg>
    <span id="jc-chat-badge">1</span>
</button>

<div id="jc-chat-window">
    <div id="jc-chat-header">
        <div id="jc-chat-avatar">🐒</div>
        <div id="jc-chat-info"><h3>Джуси</h3><p>Помощник Джунгли Сити</p></div>
        <button id="jc-chat-close" onclick="JCChat.close()">&times;</button>
    </div>
    <div id="jc-chat-messages"></div>
    <div id="jc-chat-input-area">
        <input type="text" id="jc-chat-input" placeholder="Напишите сообщение..." onkeypress="if(event.key==='Enter')JCChat.send()">
        <button id="jc-chat-send" onclick="JCChat.send()">
            <svg viewBox="0 0 24 24"><path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/></svg>
        </button>
    </div>
</div>
<!-- /JUNGLE CITY CHAT WIDGET -->
```

---

## Шаг 3: Вставить JavaScript код перед `</body>`

⚠️ **ВАЖНО**: Замените `ВАШ_ПОДДОМЕН` на реальный адрес API после настройки DNS!

```html
<script>
const JCChat = {
    // ⚠️ ЗАМЕНИТЕ НА РЕАЛЬНЫЙ URL ПОСЛЕ НАСТРОЙКИ ПОДДОМЕНА!
    // Пример: 'https://chat.nn.jucity.ru/chat'
    API_URL: 'https://ВАШ_ПОДДОМЕН/chat',
    
    sessionId: null, isOpen: false, isLoading: false,
    init: function() {
        this.sessionId = localStorage.getItem('jc_chat_session');
        setTimeout(() => { if (!this.isOpen && !localStorage.getItem('jc_chat_greeted')) { document.getElementById('jc-chat-badge').style.display = 'flex'; } }, 3000);
    },
    toggle: function() { this.isOpen ? this.close() : this.open(); },
    open: function() {
        this.isOpen = true;
        document.getElementById('jc-chat-window').style.display = 'flex';
        document.getElementById('jc-chat-badge').style.display = 'none';
        localStorage.setItem('jc_chat_greeted', 'true');
        if (!document.querySelector('.jc-message')) { this.addBotMessage('Привет! 👋 Я Джуси из Джунглей! 🐒\n\nЧем могу помочь?'); this.showQuickReplies(['🎟 Узнать о парке', '🎉 Организовать праздник', '💰 Цены на билеты']); }
        document.getElementById('jc-chat-input').focus();
    },
    close: function() { this.isOpen = false; document.getElementById('jc-chat-window').style.display = 'none'; },
    addBotMessage: function(text) { const m = document.getElementById('jc-chat-messages'); const d = document.createElement('div'); d.className = 'jc-message bot'; d.innerHTML = this.formatMessage(text); m.appendChild(d); m.scrollTop = m.scrollHeight; },
    addUserMessage: function(text) { const m = document.getElementById('jc-chat-messages'); const d = document.createElement('div'); d.className = 'jc-message user'; d.textContent = text; m.appendChild(d); m.scrollTop = m.scrollHeight; },
    showTyping: function() { const m = document.getElementById('jc-chat-messages'); const d = document.createElement('div'); d.className = 'jc-message bot typing'; d.id = 'jc-typing'; d.innerHTML = '<div class="jc-typing-dots"><span></span><span></span><span></span></div>'; m.appendChild(d); m.scrollTop = m.scrollHeight; },
    hideTyping: function() { const t = document.getElementById('jc-typing'); if (t) t.remove(); },
    showQuickReplies: function(options) { const m = document.getElementById('jc-chat-messages'); const d = document.createElement('div'); d.className = 'jc-quick-replies'; d.id = 'jc-quick-replies'; options.forEach(o => { const b = document.createElement('button'); b.className = 'jc-quick-reply'; b.textContent = o; b.onclick = () => { d.remove(); this.sendMessage(o); }; d.appendChild(b); }); m.appendChild(d); m.scrollTop = m.scrollHeight; },
    formatMessage: function(text) { text = text.replace(/(https?:\/\/[^\s]+)/g, '<a href="$1" target="_blank" style="color:#4CAF50;">$1</a>'); return text.replace(/\n/g, '<br>'); },
    send: function() { const i = document.getElementById('jc-chat-input'); const t = i.value.trim(); if (!t || this.isLoading) return; i.value = ''; this.sendMessage(t); },
    sendMessage: async function(text) {
        const qr = document.getElementById('jc-quick-replies'); if (qr) qr.remove();
        this.addUserMessage(text); this.showTyping(); this.isLoading = true; document.getElementById('jc-chat-send').disabled = true;
        try {
            const r = await fetch(this.API_URL, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({message: text, session_id: this.sessionId}) });
            const d = await r.json(); this.hideTyping(); this.addBotMessage(d.reply);
            if (d.session_id) { this.sessionId = d.session_id; localStorage.setItem('jc_chat_session', d.session_id); }
        } catch (e) { console.error('Chat error:', e); this.hideTyping(); this.addBotMessage('Ой, что-то пошло не так 😅 Попробуйте позвонить: +7 (831) 213-50-50'); }
        this.isLoading = false; document.getElementById('jc-chat-send').disabled = false; document.getElementById('jc-chat-input').focus();
    }
};
document.addEventListener('DOMContentLoaded', function() { JCChat.init(); });
</script>
```

---

## Порядок действий

1. ✅ **Создать DNS-запись** для поддомена (например, `chat.nn.jucity.ru` → `95.81.99.32`)
2. ✅ **Сообщить нам** выбранный поддомен
3. ✅ Мы **настроим SSL** и сообщим итоговый URL
4. ✅ **Вставить CSS** в `<head>`
5. ✅ **Вставить HTML и JS** перед `</body>`
6. ✅ **Заменить `API_URL`** на реальный адрес
7. ✅ **Проверить работу** виджета

---

## Контакты

- **Telegram**: @nataliabobrovskaya
- **Сервер**: 95.81.99.32
- **Порт API**: 8000

*Документ: 10 января 2026*
